# 🧠 Project Memory: Golf Scoring App

> 마지막 갱신: 2026-03-12 | 상태: Round Course-ID Auto-Repair 로직 구현 완료

## 📋 핵심 요약 (SSOT Summary)

- **Course System**: 아리스타CC 중복 코스 정리 완료 (4→2개). `resolveMergedRounds`의 `updatedAt` 승자 결정 로직으로 인해 force pull만으로 로컬 캐시 업데이트 불가 확인.
- **Stale Cache Recovery**: `loadMasterAndSession`에서 `getCourseWithHoles()` null 시 Supabase `rounds` 테이블 직접 조회 → 최신 course_id로 재시도하는 fallback 로직 추가.
- **Tab Navigation**: `currentRoundId` 기반 탭 라벨(`기록 수정`/`새 라운딩`) 및 `mode=edit|new` 분기 라우팅 구현.
- **Dashboard Dialog 제거**: `hasPromptedSession` 및 "진행 중인 라운딩 감지" Alert 완전 제거.

## 🛠️ 최근 변경 사항 (Recent Changes)

- **DB**: 아리스타CC `golf_courses` 구버전 2개("Lake Course", "Mountain Course") 삭제. `rounds.out/in_course_id`를 신버전 ID로 교체 (`updated_at = NOW()` 필수).
- **CourseSelector**: 코스/티박스 선택 단계에서 선택된 구장명을 타이틀 하단에 표시.
- **useGolfRecord**: `supabase` 정적 import 추가. course_id 만료 시 Supabase 직접 조회 fallback. `modeRef` Stable Ref Pattern 적용 → `loadMasterAndSession` 의존성에서 `mode` 제거.
- **useDashboardData**: `hasPromptedSession`, `Alert`, `Platform` import 제거. `autoSync` 의존성 배열 정리.
- **_layout.tsx**: `RecordTabButton`을 파일 스코프로 이동. `handleRecordTabPress` useCallback + `recordTabButton` useMemo로 참조 안정화. `router.push` → `router.replace` 교체.
- **admin.tsx + golf.repository**: `removeCourse` async 전환 — DB에 저장된 코스(id 보유)는 Alert 확인 후 `clubRepository.deleteGolfCourse(courseId)` 호출로 Supabase 삭제 연동. 신규(미저장) 코스는 즉시 로컬 제거.

## 🚀 다음 단계 (Next Steps)

- **[DB 주의]** SQL UPDATE 시 `updated_at = NOW()` 누락 시 `resolveMergedRounds`가 로컬 구버전을 유지 — 이후 DB 직접 수정 시 반드시 포함.

## ⚠️ 기술 채무 및 주의사항

- **Encoding**: 모든 물리 파일은 `UTF-8 no BOM` 규격 준수.

---

### Artifacts & Documents

- [CRITICAL_LOGIC.md](./CRITICAL_LOGIC.md)
- [plans/record-tab-rerender-fix.md](./plans/record-tab-rerender-fix.md)
- [plans/duplicate-courses-cleanup.md](./plans/duplicate-courses-cleanup.md)

Generated by Antigravity Senior Architect
