# 🧠 Project Memory: Golf Scoring App

> 마지막 갱신: 2026-03-12 01:26 | 상태: 안정화 완료 + SSOT 동기화 및 git push

## 📋 핵심 요약 (SSOT Summary)

- **Course System**: Task 1-4 완료. 400개 구장(약 1.4만 행) 대량 적재 테스트 성공. **Chunked Bulk Insertion(50)** 도입으로 타임아웃 해결.
- **Sync Engine**: 30분 간격 Throttling 및 Silent Sync 적용으로 UI Jank 제거.
- **Navigation Stability**: Tab 간 빠른 전환 시 발생하는 크래시 및 새로고침 현상 해결. 선언적 라벨링 및 정밀 리다이렉트 가드 적용.

## 🛠️ 최근 변경 사항 (Recent Changes)

- **Bugfix**: `admin_import.tsx` — `isSaving` useState가 early return 이후에 선언되어 Rules of Hooks 위반 발생. 함수 최상단으로 이동하여 수정. 동시에 `JSON.parse()` 반환값을 `unknown`으로 캐스팅 및 `holes: any[]` → `unknown[]` 교체로 린트 에러 2건 추가 해결.
- **UX**: `CourseSelector.tsx`에 구장 검색 기능 추가. `searchQuery` state + `filteredClubs` useMemo. 단계 전환 시 검색어 자동 초기화. 검색 결과 0건 시 맥락별 안내 메시지 분리.
- **Admin**: `clubRepository.registerClubsBulk`에 50개 단위 청크 처리 로직 추가 (DB Timeout 57014 방지).
- **Navigation**: `TabLayout`에서 `useGlobalSearchParams`를 통한 선언적 탭 라벨 제어 (`기록 수정` vs `새 라운딩`).
- **Security**: `RootLayout`의 리다이렉트 가드를 `segments[0]` 기반으로 정밀화하여 무한 루프 가능성 차단.

## 🚀 다음 단계 (Next Steps)

1. **Production QA**: 실제 운영 환경(Production DB)에서의 데이터 정합성 최종 확인.
2. **UI Polishing**: 기록 화면 내 `ScrollView` 영역의 스크롤 가독성 개선 (Hole Header 고정 등).
3. **Analytics**: 대시보드의 미스 샷 통계 로직 고도화 (최근 10경기 트렌드 추가).

## ⚠️ 기술 채무 및 주의사항

- **Performance**: 관리자 임포트 화면에서 400개 이상의 카드를 렌더링할 때 `FlatList` 도입 검토 필요.
- **Encoding**: 모든 물리 파일은 `UTF-8 no BOM` 규격을 엄격히 준수할 것.
- **Lint**: `current_problems` 기반 린트 에러 0개 상시 유지.

---

### Artifacts & Documents

- [CRITICAL_LOGIC.md](./CRITICAL_LOGIC.md)
- [navigation_stability.md](./plans/navigation_stability.md)
- [add_golf_course_system.md](./plans/add_golf_course_system.md)

Generated by Antigravity Senior Architect
